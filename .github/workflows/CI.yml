name: CI

on:
  push:
    branches: [ '*' ]
  pull_request:
    branches: [ '*' ]

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        platform: [windows-latest]
    runs-on: ${{ matrix.platform }}

    steps:
    - uses: actions/checkout@v2
    - name: action
      run: |
        echo "view:"
        attrib .github
        dir .

        echo "protect:"
        attrib +r .github
        attrib .github
        dir .

        echo "del:"
        del .github
        dir .

        echo "unprotect:"
        attrib .github
        attrib -r .github
        attrib .github

        echo "del:"
        del .github
        dir .
